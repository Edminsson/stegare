import { DirreDirective } from './dirre.directive';

describe('DirreDirective', () => {
  it('should create an instance', () => {
    const directive = new DirreDirective();
    expect(directive).toBeTruthy();
  });
});
